# rshell

A simple shell application written in Rust.

## Building

To build the project, navigate to the root directory of the project and run:

```bash
cargo build
```

## Running

To run the project, use the following command:

```bash
cargo run
```
